class Solution {
public:
    bool isSubsequence(string s, string t) {
    int i,j=0;
    for(i=0;i<t.size();i++){
        if(s[j]==t[i])
           j=j+1;
    }
    if(j!=s.size())
       return false;
    return true;
    }
};
