class Solution {
public:
    int scoreOfString(string s) {
        int i,n,sum;
        sum=0;
        n=s.size();
        for(i=0;i<n-1;i++){
            sum=sum+abs(s[i]-s[i+1]);
        }
        return sum;
    }
};
